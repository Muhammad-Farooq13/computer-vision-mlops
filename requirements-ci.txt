# CI-safe requirements
# torch/torchvision/torchaudio excluded: GPU packages (~2 GB), not available on CPU runners
# albumentations excluded: requires opencv/imgaug native libs
# black excluded: black 26.x pulls in pytokens (Rust extension) not needed in CI
numpy>=1.24
pandas>=1.5
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
Pillow>=10.0
flask>=2.3
flask-cors>=4.0
gunicorn>=21.2
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.65
requests>=2.31
joblib>=1.3
pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.11
flake8>=6.0
