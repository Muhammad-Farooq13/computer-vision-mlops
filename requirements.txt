# Streamlit Cloud / runtime dependencies
# GPU packages (torch, torchvision, albumentations, opencv) are NOT listed here
# because they are unavailable on CPU-only cloud hosts.
# Install them locally with:
#   pip install torch torchvision albumentations opencv-python
#   pip install -e .

# Core data + ML
numpy>=1.24
pandas>=1.5
scikit-learn>=1.3
Pillow>=10.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Streamlit dashboard
streamlit>=1.32
pyarrow>=12.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31
joblib>=1.3

# Web framework (needed for flask_app.py)
flask>=2.3
flask-cors>=4.0
gunicorn>=21.2
